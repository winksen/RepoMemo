---
version: 1
slug: "src-app-tsx"
primary_target: "src/App.tsx"
related_targets: ["src/styles.css"]
---

## Scope and mode

RepoMemo has two application paths inside this shell. Tauri keeps the local
desktop workbench; browser runtime is a protected shared-mode client. Visitor
mode: **Operate**.

## Audience, job, and action

Developers and technical teams use this surface to recover trustworthy context.
Local users operate a device-local workspace. Browser users authenticate,
restore their session, establish an organization, and see only shared
workspaces returned by the server.

## Content and constraints

Preserve every implemented workflow, factual label, local-first guarantee, theme mode, and desktop affordance. The interface must support long paths, code-oriented content, dense result sets, keyboard navigation, reduced motion, and both light and dark environments.

The browser client stores its JWT only in session storage. It must clearly
handle invalid credentials, expired sessions, API reachability, an empty
organization list, and first-workspace setup. It must never use preview data in
shared mode or present unavailable artifact/retrieval routes as working.

## Chosen direction

**Evidence Instrument.** Use the approved evidence-first trace canvas from composition C, the registered three-zone navigation clarity from A, and the compact source-to-memory pipeline treatment from B. Provenance is the composition: ledgers and selected records visibly resolve into evidence, status, and next action.

In browser mode, extend that system as a **Protected Evidence Instrument**:
account access precedes a composed organization rail and workspace ledger, so
the server authority is visible before knowledge artifacts arrive.

## Memorable moment

A restrained trace enters the active evidence plane and settles through source, indexed state, and durable memory. The trace is meaningful, not decorative; it always maps to real counts, stages, selection, or citations.

## Unresolved decisions

Exact responsive collapse behavior and the final density of traces should be settled through rendered implementation testing.
